{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "web search tool",
  "type": "object",
  "properties": {
    "query": {"type": "string", "minLength": 1},
    "limit": {"type": "integer", "minimum": 1, "maximum": 20},
    "source": {"type": "string", "enum": ["duckduckgo"]}
  },
  "required": ["query"],
  "additionalProperties": false
}

